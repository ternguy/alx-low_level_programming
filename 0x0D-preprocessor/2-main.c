#include <stdio.h>

/**
 * main - print the name of the file the program compile from,
 * followed by a new line
 * Return: return void
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}

